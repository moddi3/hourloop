import EStyleSheet from 'react-native-extended-stylesheet';

const styles = EStyleSheet.create({
  header: {
    color: '#333', // [TODO:] make it global
    fontWeight: 'bold',
  },
});

export default styles;
